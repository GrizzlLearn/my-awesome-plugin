# Mail Viewer Servlet - Refactored Architecture

## Overview

Рефакторинг `MailViewerServlet` был выполнен для улучшения читаемости, поддерживаемости и разделения обязанностей. Исходный сервлет был слишком перегружен различными задачами.

## Новая архитектура

### 1. `MailViewerServlet` (Main Controller)
**Обязанности:**
- Роутинг HTTP запросов
- Аутентификация и авторизация
- Обработка исключений на верхнем уровне
- Инициализация компонентов

**Ключевые особенности:**
- Минимальная бизнес-логика
- Делегирование специализированным классам
- Чистая структура методов

### 2. `MailItemRequestHandler` (Request Processing)
**Обязанности:**
- Обработка JSON API запросов
- Форматирование ответов
- Обработка ошибок на уровне API
- Создание стандартизированных JSON ответов

**Основные методы:**
- `handleDataRequest()` - получение данных в JSON
- `handleDeleteAllRequest()` - удаление всех элементов
- `handleCreateTestDataRequest()` - создание тестовых данных
- `handleForbiddenRequest()` / `handleNotFoundRequest()` - обработка ошибок

### 3. `MailItemPageRenderer` (View Layer)
**Обязанности:**
- Рендеринг HTML страниц
- Обработка шаблонов (Velocity)
- Обслуживание статических ресурсов (CSS)
- Интеграция с WebResourceManager

**Основные методы:**
- `renderMainPage()` - главная навигационная страница
- `renderTablePage()` - страница с таблицей
- `serveCssFile()` - обслуживание CSS файлов

### 4. `TestDataInitializer` (Utility)
**Обязанности:**
- Инициализация тестовых данных
- Проверка состояния данных
- Логирование операций с тестовыми данными

**Основные методы:**
- `initializeIfEmpty()` - создание данных при необходимости
- `forceCreateTestData()` - принудительное создание
- `isDataEmpty()` / `getMailItemsCount()` - проверка состояния

### 5. `MailViewerConstants` (Configuration)
**Обязанности:**
- Централизованное хранение констант
- URL паттерны и пути к ресурсам
- Сообщения об ошибках и успехе
- Типы контента

## Преимущества нового подхода

### 🎯 Разделение обязанностей (Single Responsibility Principle)
- Каждый класс имеет четко определенную роль
- Упрощает тестирование и отладку
- Облегчает понимание кода

### 🔧 Улучшенная поддерживаемость
- Изменения в одной области не затрагивают другие
- Легче добавлять новый функционал
- Упрощенное исправление багов

### 📝 Читаемость кода
- Короткие, сфокусированные методы
- Понятные названия классов и методов
- Централизованные константы

### 🧪 Тестируемость
- Компоненты легко мокать
- Изолированная бизнес-логика
- Четкие интерфейсы между компонентами

### 🚀 Расширяемость
- Новые типы запросов легко добавлять
- Простая интеграция новых рендереров
- Гибкая система констант

## Структура файлов

```
com/noname/plugin/servlet/
├── MailViewerServlet.java           # Основной контроллер
├── MailViewerConstants.java         # Константы и конфигурация
├── handler/
│   └── MailItemRequestHandler.java  # Обработка API запросов
├── renderer/
│   └── MailItemPageRenderer.java    # Рендеринг страниц
└── util/
    └── TestDataInitializer.java     # Утилиты для тестовых данных
```

## Миграция со старой версии

### Что изменилось:
1. **Основной сервлет** стал значительно меньше и сфокусирован на роутинге
2. **Обработка запросов** вынесена в отдельный handler
3. **Рендеринг** отделен от бизнес-логики
4. **Константы** централизованы
5. **Тестовые данные** управляются отдельным компонентом

### Что осталось без изменений:
1. **Публичные URL** и API endpoints
2. **Функциональность** - все работает как раньше
3. **Зависимости** - те же Spring компоненты
4. **Конфигурация** в atlassian-plugin.xml

## Пример использования

### Добавление нового endpoint'а:

1. **Добавить константу** в `MailViewerConstants`:
```java
public static final String NEW_ENDPOINT = "/new-feature";
```

2. **Добавить роутинг** в `MailViewerServlet`:
```java
} else if (requestURI.endsWith(NEW_ENDPOINT)) {
    requestHandler.handleNewFeatureRequest(resp);
```

3. **Реализовать handler** в `MailItemRequestHandler`:
```java
public void handleNewFeatureRequest(HttpServletResponse resp) throws IOException {
    // логика обработки
}
```

### Добавление новой страницы:

1. **Добавить метод** в `MailItemPageRenderer`:
```java
public void renderNewPage(HttpServletRequest req, HttpServletResponse resp) throws IOException {
    // логика рендеринга
}
```

2. **Обновить роутинг** в сервлете для вызова нового рендерера

## Заключение

Рефакторинг значительно улучшил архитектуру кода, сделав его более модульным, читаемым и поддерживаемым. Каждый компонент теперь имеет четко определенные обязанности, что упрощает разработку и тестирование.
