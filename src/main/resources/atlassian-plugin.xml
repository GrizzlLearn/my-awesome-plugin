<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="mail-catcher"/>
    
    <!-- add our web resources -->

    <!-- Mail Table Page -->
    <web-resource key="mail-table-resources" name="Mail Table Resources">
        <resource type="download" name="mail-table.css" location="css/mail-table.css"/>
        <context>atl.general</context>
    </web-resource>

    <!-- Mail Data Page -->
    <ao key="ao-mail-item-module" name="Active Objects Module">
        <entity>com.noname.plugin.ao.MailItemEntity</entity>
    </ao>
    <!-- Servlet конфигурация -->
    <servlet name="Mail Catcher"
             key="mail-viewer-servlet"
             class="com.noname.plugin.servlet.MailViewerServlet">
        <description>Capture and inspect outgoing emails</description>
        <url-pattern>/mail-items</url-pattern>
        <url-pattern>/mail-items/*</url-pattern>
    </servlet>

    <!-- Web конфигурация -->
    <web-section key="mail_catcher_config_section"
                 location="admin_plugins_menu"
                 weight="1000">
        <label key="plugin.name"/>
    </web-section>
    <web-item key="web.item.mail.viewer.servlet"
              section="admin_plugins_menu/mail_catcher_config_section"
              weight="40">
        <label key="mail.viewer.servlet"/>
        <link>/plugins/servlet/mail-items/</link>
    </web-item>

    <!-- Active Objects конфигурация -->

    <!-- Component конфигурация -->

</atlassian-plugin>
